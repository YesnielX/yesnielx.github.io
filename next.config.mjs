/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/portfolio',
    assetPrefix: '/portfolio',
    output: "export",
    reactStrictMode: true,
};

export default nextConfig;
